'use strict';


let imageData = new ImageData(0.0, 0.0);

let arr = new Uint8ClampedArray([]);

new ImageData(arr, 0.0, 0.0);

exports.imageData = imageData;
exports.arr = arr;
/* imageData Not a pure module */
